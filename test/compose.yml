services:
  postgres:
    container_name: test_http
    image: postgres:latest
    environment:
      POSTGRES_USER : 'user'
      POSTGRES_PASSWORD : 'password'
      POSTGRES_DB : 'test_db'
    ports:
      - "5433:5432"
